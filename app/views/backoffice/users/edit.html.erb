<div class="container" style="height: 100%; padding-top: 2%;">
  <div class="row">
    <div class="col-md-12 mx-auto margin-bottom: 15px;">
      <div class="card">
        <div class="card-header d-flex justify-content-between align-items-center">
          <div>Editar Usuário</div>
        </div>
        <div class="card-body">
          <%= form_with(model: @user, url: backoffice_user_path(@user), method: :patch) do |form| %>
            <%= render 'form', form: form %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
